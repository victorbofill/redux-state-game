import React, { Component } from 'react';
import Table from './Table';

export default class Game extends Component {
  render() {
    return (
      <section>
        <Table/>
      </section>
    );
  }
}